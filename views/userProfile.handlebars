<div class="hero-body">
    <p class="subtitle">Name: {{oneUser.name}}</p>
    <p class="subtitle">Email: <a href="mailto:{{oneUser.email}}">{{oneUser.email}}</a></p>
    <p><a href="/api/reviews/users/{{oneUser.id}}">See all reviews by {{oneUser.name}}</a></p>
</div>
